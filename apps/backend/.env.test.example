# Test Database Configuration
# Database name is automatically derived from POSTGRES_DB in docker-compose.yml
# (e.g., monorepo_dev -> monorepo_test, myapp_dev -> myapp_test)
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/monorepo_test?sslmode=disable

# JWT Secret for tests
JWT_SECRET=test-secret-key-do-not-use-in-production
